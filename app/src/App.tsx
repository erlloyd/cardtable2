import { CARDTABLE_VERSION } from '@cardtable2/shared';

function App() {
  return (
    <div className="app">
      <header className="app-header">
        <h1>Cardtable {CARDTABLE_VERSION} - App Test</h1>
        <p>
          Solo-first card table with multiplayer support (app-only change test)
        </p>
      </header>
    </div>
  );
}

export default App;
